# host port for the site (container side is always 8080 -> unprivileged nginx)
FRONTEND_PORT=80
# image naming
DOCKER_USER=noble6
TAG=latest
VERSION=dev
# build-time frontend knobs
VUE_APP_API_URL=/api
VUE_APP_PUBLIC_PATH=/
